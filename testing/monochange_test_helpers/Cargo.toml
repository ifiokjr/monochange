[package]
name = "monochange_test_helpers"
version = "0.0.0"
edition = "2024"
license = "Unlicense"
publish = false
description = "Internal shared test helpers for monochange"

[dependencies]
insta = { version = "1", default-features = false, features = ["filters", "json"] }
rmcp = { version = "1", default-features = false }
tempfile = { version = "3", default-features = false }
