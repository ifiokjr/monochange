---
monochange: patch
---

# split CLI internals into reusable modules

This release keeps the existing `mc assist`, `mc release-record`, retargeting, and release-commit behavior the same while reorganizing the implementation into smaller reusable modules for assistant setup, git helpers, and release-record workflows.
