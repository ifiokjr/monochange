---
monochange: patch
---

# split monochange CLI runtime internals further

`monochange` now keeps CLI step execution, template-context building, and retarget-report rendering in a dedicated runtime module. This keeps the public CLI behavior the same while making the top-level crate entry point smaller and easier to navigate.
