---
monochange: minor
monochange_config: minor
monochange_core: minor
---

Add the `RetargetRelease` CLI step and the default `mc repair-release` command for repairing a recent release by moving its release tags to a later commit.
