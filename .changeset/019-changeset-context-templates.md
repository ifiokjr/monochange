---
monochange: patch
monochange_core: patch
monochange_config: patch
monochange_github: patch
---

Rename authored changeset metadata from `provenance` to `context`, keep `$provenance` as a compatibility alias, add fine-grained changelog template variables for linked review and issue metadata, and document readable release-note template defaults.
