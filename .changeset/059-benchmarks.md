---
monochange: patch
---

Add criterion benchmarks for discover and prepare_release with varying workspace sizes (5-50 packages, 10-200 changesets). Add benchmark CI job that runs on every PR and uploads results as artifacts.
