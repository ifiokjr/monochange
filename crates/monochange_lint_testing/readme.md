# monochange_lint_testing

Testing helpers for monochange lint suites.

The helpers in this crate format lint reports and autofix output in stable text shapes so suite tests can snapshot them with `insta`.
