[defaults]
parent_bump = "patch"
package_type = "cargo"

[package.core]
path = "crates/core"
changelog_sections = [{ name = "Security", types = ["security"], bump = "patch" }]

[package.app]
path = "crates/app"

[group.sdk]
packages = ["core", "app"]
changelog_sections = [{ name = "Testing", types = ["test"], bump = "minor" }]
