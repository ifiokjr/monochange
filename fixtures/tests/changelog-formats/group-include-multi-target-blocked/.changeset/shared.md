---
core: minor
app: patch
---

#### add shared release note

This note touches both the public and internal packages, so it should not appear in the grouped changelog when only one member is allowlisted.
