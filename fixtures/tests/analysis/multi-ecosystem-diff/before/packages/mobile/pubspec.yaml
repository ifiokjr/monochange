name: mobile_app
version: 1.0.0

environment:
  sdk: ^3.4.0

dependencies:
  flutter:
    sdk: flutter

executables:
  mobile-app:

flutter:
  plugin:
    platforms:
      android:
        package: com.example.mobile
