#!/bin/sh
set -eu
version=$(awk -F '"' '/"version"/ { print $4; exit }' package.json)
printf 'custom-ran' > custom-ran.txt
cat > package-lock.json <<EOF
{
  "name": "app",
  "version": "${version}-custom",
  "lockfileVersion": 3
}
EOF
